##
## Файл ~/.ssh/config
##

# Настройки для всех хостов.
Host *
	# Отключаем проверку ключа хоста, чтобы избежать запроса
	# подтверждения при первом подключении к новому хосту.
    StrictHostKeyChecking no

    # Отключаем сохранение известных хостов, чтобы избежать
    # отказа в подключении при изменении ключа хоста.
    UserKnownHostsFile=/dev/null
